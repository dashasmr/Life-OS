import { ds } from "@/styles/design-system";

/** Shared horizontal shell: aligns top bar with page content (max 1440px, responsive padding). */
export const PAGE_SHELL_CLASS = ds.surfaces.pageShell;
